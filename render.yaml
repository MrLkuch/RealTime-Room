services:
  - type: web
    name: realtime-api
    runtime: docker
    rootDir: api
    dockerfilePath: Dockerfile
    branch: main
    plan: free
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production

  - type: web
    name: realtime-web
    runtime: docker
    rootDir: web
    dockerfilePath: Dockerfile
    branch: main
    plan: free
    autoDeploy: true
    envVars:
      - key: VITE_API_URL
        value: https://realtime-api-bt0a.onrender.com
